# Python dependencies for the LLM Wiki scaffold.
#
# Install with: pip install -r requirements.txt
#            or: uv pip install -r requirements.txt

# Used by scripts/wiki-lint.py for frontmatter parsing.
PyYAML

# Used by /wiki-ingest to convert PDF sources to markdown.
# Invoked inline as: python -c "import pymupdf4llm; ..."
pymupdf4llm
